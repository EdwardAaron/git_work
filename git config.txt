git config --list --show-origin
	显示配置文件信息

设置用户
	git config --global user.name EdwardAaron
	git config --global user.email 467200839@qq.com

设置默认编辑器（sublime-需要将sublime安装目录，添加到path中）
	git config --global core.editor "subl -w"
检查配置
	git config --list

检查某一下
	git config <key>，例如 git config user.name